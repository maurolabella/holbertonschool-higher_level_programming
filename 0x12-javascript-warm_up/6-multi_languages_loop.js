#!/usr/bin/node
// like 1-multi_languaje.js, but by using an array of strings
// and a loop
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (const line of lines) {
  console.log(line);
}
